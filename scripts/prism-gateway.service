[Unit]
Description=PRISM Agent Gateway
After=network.target

[Service]
Type=simple
User=%i
WorkingDirectory=/opt/prism
Environment=HOME=%h
Environment=PATH=/usr/local/bin:/usr/bin:/bin
ExecStart=/usr/local/bin/prism gateway start
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
